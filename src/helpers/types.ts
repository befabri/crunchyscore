export enum ScoreType {
    CARD = "card",
    DETAIL = "detail",
    WATCH = "watch",
}

export enum ScoreSelector {
    CARD = ".score-card",
    DETAIL = ".score-detail",
    WATCH = ".score-watch",
}
